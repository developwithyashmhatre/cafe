<Window x:Class="my_cyber_cafe.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" WindowStartupLocation="CenterScreen" Background="#18181A" ResizeMode="NoResize" WindowStyle="None" WindowState="Maximized" AllowsTransparency="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Please login to continue:" Foreground="White" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Welcome" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,0"/>
           <TextBlock x:Name="LoginSubText" Text="Please login to continue" Foreground="#b3b3b3" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,24"/>

            <Border Background="#232326" CornerRadius="16" Padding="32" >
                <StackPanel>
                    <TextBlock Text="Login" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                    <TextBlock Text="Enter your credentials to continue" Foreground="#b3b3b3" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="User name" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,12" Height="36">
                        <TextBox x:Name="EmailBox" Height="36" FontSize="14" Background="#18181A" Foreground="White" BorderBrush="#333" BorderThickness="1" Padding="10,0"/>
                        <TextBlock Text="Enter your username" Foreground="#666" FontSize="14" Padding="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=EmailBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text, ElementName=EmailBox}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text, ElementName=EmailBox}" Value="{x:Static sys:String.Empty}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text, ElementName=EmailBox}" Value="*">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    <TextBlock Text="Password" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,20" Height="36">
                        <PasswordBox x:Name="PasswordBox" Height="36" FontSize="14" Background="#18181A" Foreground="White" BorderBrush="#333" BorderThickness="1" Padding="10,0"/>
                        <TextBlock Text="Enter your password" Foreground="#666" FontSize="14" Padding="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Password, ElementName=PasswordBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Password, ElementName=PasswordBox}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Password, ElementName=PasswordBox}" Value="*">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    <Button x:Name="LoginBtn" Content="Login" Height="40" Background="#FF3B3B" Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0" Margin="0,0,0,0" Cursor="Hand"/>
                    <TextBlock x:Name="ErrorText" Foreground="Red" Margin="0,10,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window> 